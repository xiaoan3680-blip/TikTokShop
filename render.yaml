services:
  - type: web
    name: tiktokshop
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      NODE_ENV: production
    autoDeploy: true
